const initialState = {
  firstName: 'Bob',
  lastName: 'Hope',
  username: 'bob@hope.com'
}

const currentUserReducer = (state, action) => {
  return initialState
}

export default currentUserReducer;